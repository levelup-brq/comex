#spring.datasource.url=${COMEX_DATABASE_URL}
#spring.datasource.username=${COMEX_DATABASE_USERNAME}
#spring.datasource.password=${COMEX_DATABASE_PASSWORD}

spring.datasource.url=jdbc:mariadb://127.0.0.1:3306:3306/comex
spring.datasource.username=root
spring.datasource.password=123456

spring.datasource.testWhileIdle=true
spring.datasource.validationQuery=SELECT 1

spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

#Mostrar o SQL no console
spring.jpa.show-sql=true

spring.jpa.hibernate.ddl-auto=update
spring.jpa.hibernate.naming-strategy=org.hibernate.cfg.ImproveNamingStrategy
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect

#Mostrar o SQL no console
spring.jpa.properties.hibernate.show_sql=true

#Formatar o SQL no console
spring.jpa.properties.hibernate.format_sql=true

# Configuraçao para o swagger
spring.mvc.pathmatch.matching-strategy=ant-path-matcher

# jwt
#forum.jwt.secret=${COMEX_JWT_SECRET}
forum.jwt.secret=123456
forum.jwt.expiration=86400000

# actuator
management.endpoint.health.show-details=always
management.endpoints.web.exposure.include=*
info.app.name=@project.name@
info.app.description=@project.description@
info.app.version=@project.version@
info.app.encoding=@project.build.sourceEncoding@
info.app.java.version=@java.version@ 

#Heroku
#server.port=${PORT}